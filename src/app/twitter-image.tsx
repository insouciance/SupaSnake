/**
 * The Twitter/X card. Deliberately the same artwork as the Open Graph card
 * (`summary_large_image` uses the same 1200×630 frame), so there is one
 * design to keep true rather than two that drift.
 */

export { alt, size, contentType, default } from './opengraph-image';
